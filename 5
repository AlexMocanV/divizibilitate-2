#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n,i=0;
    cin>>n;
    while(i*i<n)
    {
        i++;
    }
    if(i*i==n)
    {
        cout<<"da";
    }
    else
    {
        cout<<"nu";
    }

    return 0;
}
